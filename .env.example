# 🚀 Vinsmoke Backend - Production Configuration

# Required: Your frontend URL (after deploying to Vercel)
FRONTEND_URL=https://your-frontend.vercel.app

# Required: GitHub OAuth for admin authentication
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret
BACKEND_URL=https://your-backend.onrender.com

# Optional: Advanced Configuration (has smart defaults)
SESSION_TIMEOUT=3600000          # Session timeout in ms (1 hour)
MAX_SESSIONS=100                 # Maximum concurrent sessions
PLUGIN_CACHE_TTL=300000         # Plugin cache TTL in ms (5 minutes)
RATE_LIMIT_WINDOW=900000        # Rate limit window in ms (15 minutes)
RATE_LIMIT_MAX=100              # Max requests per window

# Optional: Multiple frontend domains (comma-separated)
# ALLOWED_ORIGINS=https://staging.your-app.com,https://www.your-app.com
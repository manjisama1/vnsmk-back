# 🚀 Vinsmoke Backend Environment Configuration

# Server Configuration (Required)
NODE_ENV=production
PORT=8080

# CORS Configuration (Required for frontend)
FRONTEND_URL=https://your-frontend-domain.com

# Security & Performance (Optional - has defaults)
TRUST_PROXY=true
REQUEST_TIMEOUT=30000
MAX_REQUEST_SIZE=10mb
RATE_LIMIT_WINDOW=900000
RATE_LIMIT_MAX=100

# GitHub OAuth (Optional - only needed for admin features)
GITHUB_CLIENT_ID=your_github_client_id_here
GITHUB_CLIENT_SECRET=your_github_client_secret_here
BACKEND_URL=https://your-backend-domain.com

# Advanced CORS (Optional - uses FRONTEND_URL if not set)
# ALLOWED_ORIGINS=https://your-frontend.com,https://www.your-frontend.com

# Server Host (Optional - defaults to 0.0.0.0)
# HOST=0.0.0.0